## Customize the test machine
machine:
  timezone:
    Asia/Ho_Chi_Minh # Set the timezone
  xcode:
    version: "7.3"
  ruby:
    version: rbx-2.2.6
  # environment:
    # please specify your code signing identity name here
    # GYM_CODE_SIGNING_IDENTITY: "iPhone Distribution: Acme Inc. (GL31ZZ3256)"

dependencies:
    override:
        # - gem update
        - sudo gem install calabash-cucumber
        - sudo gem install cocoapods
        - bundle install
        # - gem list
        - brew install node
        - node -v
        - npm -v
        - npm install -g express
        - ls
        - features/server/cliclick kd:cmd t:t
        - sleep 5
        - cd features/server
        - node server.js
        - ./cliclick kd:cmd t:{
        - curl http://127.0.0.1:8081/listUsers


test:
    pre:
        # - pod setup
        # - pod install
    post:
        # - cucumber
        - bundle exec cucumber
